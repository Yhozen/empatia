import { green, pink, blue } from '@material-ui/core/colors'

const APP_COLORS = {
  needed: green[500],
  offered: pink[500],
  tip: blue[500]
}
const HEADER_HEIGHT = 80
const BOTTOM_BAR_HEIGHT = 56
export { HEADER_HEIGHT, BOTTOM_BAR_HEIGHT, APP_COLORS }
